@page "/"

@* @if (_deviceRepository.TemperatureDevices is not null)
{
	<div class="flex gap-2 flex-wrap">
		@foreach (var item in _deviceRepository.TemperatureDevices)
		{
			<TemperatureSensor DeviceName=@item.Name
							   Humidity=@item.Humidity
							   Temperature=@item.Temperature />
		}
	</div>
} *@

@if (_deviceRepository.DashboardView is not null)
{
	<div class="flex gap-2 flex-wrap">
		@foreach (var item in _deviceRepository.DashboardView.TemperatureSensors)
		{
			<TemperatureSensor DeviceName=@item.Name
							   Humidity=@item.Current.Humidity
							   Temperature=@item.Current.Temperature />
		}
	</div>
}